# Only few nodes to see what's going on
Group.nrofHosts = 4

#Setting for MessageEventsGenerator that adds broadcasts messages.
Events1.differentmessagetypes = true
# Only few messages to see what's going (one new message every 30 mins).
Events1.interval = 1800,1800
# Adapt range of message source/destination addresses to fewer hosts.
Events1.hosts = 0,17